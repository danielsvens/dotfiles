# dotfiles

## Palette switching

- Waybar palette entrypoint: `waybar/style/palette.css`
- Hyprland palette entrypoint: `hypr/config/palette/current.conf`

Both files contain Olive (active) and a commented fallback palette in the same file.
To switch palettes, comment the active block and uncomment the fallback block.
